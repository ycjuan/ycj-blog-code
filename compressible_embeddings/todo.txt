* get memory address polymorphism
* revisit copy kernel
* handle data type template
* handle versioning
* handle the logic that update should invalid cache
* think about if 1.1 * kStdDev makes sense or not
* handle compressed polymorphism
* use GPU for nearest centroid assignment